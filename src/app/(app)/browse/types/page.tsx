import { DocumentTypesFacetScreen } from '../../../../web/screens/facets';

// /browse/types — the shelf arranged by what a document is (docs/11 §11.4).
export default function BrowseTypesPage() {
  return <DocumentTypesFacetScreen />;
}
