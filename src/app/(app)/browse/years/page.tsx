import { YearsFacetScreen } from '../../../../web/screens/facets';

// /browse/years — the shelf arranged by the date on the paper, not the day it was filed.
export default function BrowseYearsPage() {
  return <YearsFacetScreen />;
}
