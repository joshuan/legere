import { PeopleFacetScreen } from '../../../../web/screens/facets';

// /browse/people — the shelf arranged by who a document is about (docs/11 §11.4).
export default function BrowsePeoplePage() {
  return <PeopleFacetScreen />;
}
