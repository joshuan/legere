import { SubjectKindsFacetScreen } from '../../../../web/screens/facets';

// /browse/subjects — kinds first, because a kind is what a folder is (docs/03 §3.3.20).
export default function BrowseSubjectKindsPage() {
  return <SubjectKindsFacetScreen />;
}
