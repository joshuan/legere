import { ResetScreen } from '../../../web/screens/reset';

// Route file: composition only (docs/10 §10.1). The credential reaches only the client fragment.
export default function ResetPage() {
  return <ResetScreen />;
}
